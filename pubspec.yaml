name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.2.2  # To store simple user data
  fl_chart: ^0.70.2  # For charts and data visualization
  sqflite: ^2.2.6  # For local database storage
  path_provider: ^2.1.2  # To get the storage path for the database

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Add your assets here
  assets:
    - assets/Logos/log.png
    - assets/Images/background.jpg
